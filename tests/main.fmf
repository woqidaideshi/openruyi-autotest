# 所有测试用例的共享配置
# 子测试目录会自动继承这些配置

# 默认测试框架
framework: shell

# 默认测试超时时间
duration: 5m

# 默认联系人
contact: QA Team <qa@example.com>

# 默认启用
enabled: true

# 默认测试执行路径
path: /

# tier 分级说明:
#   tier: 0 = 冒烟测试 (每次提交必跑)
#   tier: 1 = 核心功能/安全测试 (每日构建)
#   tier: 2 = 兼容性/性能/可靠性测试 (发布前)